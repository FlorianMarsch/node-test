
<div
layout="row"
  ng-show="active == 'transfermarkt'">
  Deine Manschaft enthält {{squad.players.length}} Spieler.<br>
Du hast noch ein Budget von {{squad.money}} Copa Credits zur Verfügung.
</div>
<div
layout="row"
ng-show="active == 'transfermarkt'">
<md-list flex>
      <md-subheader class="md-no-sticky">{{squad.players.length}} Spieler</md-subheader>
      <md-list-item class="md-3-line" ng-repeat="player in players" ng-click="null">
        <img ng-src="{{player.imageUrl}}" class="md-avatar" alt="{{player.name}}" />
        <div class="md-list-item-text" layout="column">
          <h3>{{ player.name }}</h3> 
          <h4>{{ player.position }}</h4>
          <p>{{ player.points}} Punkte {{ player.lastUse ? ', Letzter Einsatz : '+player.lastUse : ''}}</p>
        </div>
       
  </md-list-item>
</md-list>
</div>
