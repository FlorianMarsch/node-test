
<div
layout="row"
  ng-show="active == 'transfermarkt'">
  Der Transfermarkt enthält {{players.length}} Spieler.<br>
  Du hast noch ein Budget von {{squad.money}} Copa Credits zur Verfügung.
</div>
<div
layout="row"
ng-show="active == 'transfermarkt'">
<md-grid-list flex>
      <md-subheader class="md-no-sticky">{{players.length}} Spieler</md-subheader>
      <md-grid-tile md-colspan="1" md-colspan-sm="3" md-colspan-md="5"
      class="md-3-line" ng-repeat="player in players" ng-click="null">
        <img ng-src="{{player.imageUrl}}" class="md-avatar" alt="{{player.name}}" />
        <div class="md-list-item-text" layout="column">
          <h3>{{ player.name }}</h3> 
          <h4>{{ player.position }}</h4>
          <p>{{ player.points}} Punkte {{ player.lastUse ? ', Letzter Einsatz : '+player.lastUse : ''}}</p>
        </div>
       
  </md-grid-tile>
</md-list>
</div>
